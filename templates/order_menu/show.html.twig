{% extends 'base.html.twig' %}

{% block title %}OrderMenu{% endblock %}

{% block body %}
    <h1>OrderMenu</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('app_order_menu_index') }}">back to list</a>

    <a href="{{ path('app_order_menu_edit', {'menu': order_menu.menu}) }}">edit</a>

    {{ include('order_menu/_delete_form.html.twig') }}
{% endblock %}
