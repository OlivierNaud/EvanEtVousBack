{% extends 'base.html.twig' %}

{% block title %}Van{% endblock %}

{% block body %}
    <h1>Van</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ van.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ van.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ van.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ van.img }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ van.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_van_index') }}">back to list</a>

    <a href="{{ path('app_van_edit', {'id': van.id}) }}">edit</a>

    {{ include('van/_delete_form.html.twig') }}
{% endblock %}
